# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# AWS SAM / CDK CLI (install separately via brew or pip)
# brew install aws-sam-cli
# pip install aws-cdk-lib

# Bedrock / Agent SDK
strands-agents>=0.1.0
anthropic>=0.25.0

# DynamoDB local testing
boto3[dynamodb]>=1.34.0

# Lambda packaging
aws-lambda-powertools>=2.30.0

# Notebooks
jupyterlab>=4.0.0
ipykernel>=6.0.0

# Data / validation
pydantic>=2.0.0
python-dateutil>=2.8.0

# HTTP / API calls (Polygon.io, Alpaca, Plaid)
requests>=2.31.0
httpx>=0.27.0

# Environment / secrets
python-dotenv>=1.0.0
