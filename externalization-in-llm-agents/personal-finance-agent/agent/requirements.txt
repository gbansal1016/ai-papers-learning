# FinAgent Lambda dependencies
# SAM reads this file from CodeUri (agent/) and pip installs into the Lambda package.
# Do NOT add dev/notebook deps here — keep this list minimal for cold start performance.

# AWS SDK (Lambda runtime includes boto3 but pinning ensures consistent version)
boto3>=1.34.0
botocore>=1.34.0

# Strands Agent SDK + Bedrock model
strands-agents>=0.1.0
anthropic>=0.25.0

# Lambda observability
aws-lambda-powertools>=2.30.0

# Data validation
pydantic>=2.0.0
python-dateutil>=2.8.0

# HTTP (Polygon.io, Plaid API calls)
requests>=2.31.0
httpx>=0.27.0

# Alpaca trading API (official newer SDK — no sdist dependency issues)
alpaca-py>=0.8.0

# Plaid (bank account aggregation)
plaid-python>=14.0.0
